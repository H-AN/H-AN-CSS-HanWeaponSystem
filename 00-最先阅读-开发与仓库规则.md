# 00 最先阅读：开发与仓库规则

> 本文件是本目录的**第一优先级规则**，进行任何操作前必须先读并遵守。
> 适用：HanWeaponSystem 插件开发的所有版本迭代。

---

## 核心规则：开发环境是源头，git 仓库只是存档

1. **git 仓库不是开发环境**。仓库永远落后于开发环境，不需要、也不要去检查仓库是否为源头、是否与开发环境一致。

2. **只在开发环境做所有事情**：
   - 修改代码
   - 编译（`compile.exe`，零警告零报错）
   - 测试、上服验证
   - 直到我认为（华仔 H-AN）在开发环境内确认 OK、完成。

3. **完成后才同步仓库**：我在开发环境确认 OK 后，将开发环境的当前进度**全量复制**到 git 仓库存档，并提交（commit）。

4. **不需要比对差异**：仓库内容无论与开发环境差多少、旧了多少，都不用管。复制即覆盖，仓库以开发环境为准。

---

## 开发环境位置

- 代码：`E:\css\sp\sourcemod1.12 加枪\addons\sourcemod\scripting\`
- 文档：`E:\css\sp\sourcemod1.12 加枪\文档\`

## git 仓库位置

- `E:\css\CS2\steamcmd\steamapps\common\cssharp\chajian\swiftlys2-master\plugins\git\H-AN-CSS-HanWeaponSystem`
- 本地分支 `main`，远程 `origin/main`

---

## 流程速记

```
开发环境 修改 → 编译零警告零报错 → 测试 OK → 全量复制到 git 仓库 → commit
```

只有「复制 → commit」这一步与 git 相关，其余全部在开发环境完成。